import { useTheme } from "@/app/hooks/themeContext";

const ResumeDetail = () => {
    const { theme} = useTheme();
  return (
    <>
      <div>이력서 상세화면</div>
    </>
  );
};

export default ResumeDetail;
